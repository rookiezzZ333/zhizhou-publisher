# 微信公众号网页模式

网页模式使用项目专用 Microsoft Edge，无需 AppSecret。首次使用需在专用窗口扫码登录。

## 使用步骤

1. 在工作台选择文章，公众号方式选择「网页模式」。
2. 点击「自动填入公众号编辑器」。未登录时按提示扫码并确认账号，也可以先打开登录窗口。
3. 程序填写标题、作者、摘要，把 Markdown 转为微信兼容的富文本，按正文标记插入图片，并尝试设置封面。
4. 在微信页面核对正文排版、图片和封面裁剪。
5. 可使用「填入并尝试保存草稿」，或直接在微信页面保存、发表。平台验证由本人完成。
6. 如果已经手动发布，在工作台发送记录中确认发布结果。

作者来自 article.md 配置区的 author。未填写时不会猜测署名。自动操作会核对内容；遇到结构检测、控件不唯一或内容不一致时停止，避免错误填写。

## 填入不等于发布

「已填入，待核对」不代表草稿保存或正式发表。没有得到明确保存结果时需到微信后台检查，不要盲目重试。

已有正文不会被随意覆盖。遇到半成品请先检查现有编辑内容；文章标题等字段与当前内容冲突时，程序会停止并提示。

浏览器和凭证数据、现场截图、结构诊断保存在本机 .state/ 内，不应提交到公开仓库。

## 排版与兼容性

Markdown 转换使用独立行内文字节点、明确行高与内联样式。粘贴后等待微信异步处理，再开始插图；结构检测弹窗出现时不自动点击「继续插入」。

浏览器操作依赖当前网页结构。不同账号、平台更新和封面裁剪对话框都可能影响适配，需要在平台检查实际效果。本地模拟测试不能替代真实账号验证。

原来的官方接口模式仍可选择，但需配置自己的 AppID、AppSecret、IP 白名单和接口权限。

参考及历史排版修复记录见 [REFERENCES.md](REFERENCES.md) 与 [TESTING.md](TESTING.md)。
