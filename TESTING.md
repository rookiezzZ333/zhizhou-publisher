## B 站上传入口修复（2026-09-22）

真实失败现场存在两个 accept 包含 .mp4 的文件控件，其中一个隐藏、一个可见，另有 .txt 字幕控件。旧逻辑要求视频控件总数为 1，导致未执行上传。

现优先选择跨主页面 / iframe 唯一可见的视频控件；全部隐藏时仅接受唯一候选。多个可见入口仍停止，不猜测。回归页面覆盖与现场一致的隐藏副本和字幕控件，验证仅可见视频控件接收文件，随后填写文案、标签且不投稿。

视频库与 B 站相关测试 9 项通过。本次实际视频上传需用户确认后验证，不能以模拟测试替代线上成功。

# 0.7 独立视频库与 B 站（2026-09-22）

完整测试：63 项通过。覆盖新建独立视频、上传和无效替换保护、封面、Range 播放、权限和越界路径、平台文案隔离、旧视频迁移及重复保护、回收删除后不重新迁入。

B 站使用本地模拟创作页验证实际 DOM 交互：视频文件输入、标题和简介回读、标签提交、内嵌 iframe、已有编辑保护、重复打开不覆盖，且不点击最终投稿。

隔离工作台 UI 验证：1920、1280、1024、768、390 像素宽度无横向溢出；文章 / 视频平台分离；未保存文案切换提示；真实 MP4 播放和跳转进度通过。截图保存在本机 .state/ui-qa/，不提交。

B 站真实账号登录、上传转码与当前线上选择器尚未验证，不将模拟测试当作真实投稿成功。

---

> 本文包含按开发阶段记录的历史验证结果；其中“尚未实测”等描述属于当时记录。当前使用方式以 README 为准，当前测试结果以本地执行为准。

# 验证记录

日期：2026-09-21。

40 项自动化测试通过。运行：`.venv\Scripts\python.exe -m unittest discover -s tests -v`。其中 1 项需要已构建的可选上游排版检测器；未构建时明确跳过。

## 0.5：真实文章的结构检测回归

使用 wechatjs/verify-article-structure-spec 的 0.2.16 浏览器引擎（提交 fa69e37341c86bfec4c8c533845910511e534c0c），在本机无头 Edge 中运行全规则检测。测试输入为本项目 Gemini 4 深度稿，不是模拟的短文章。网络请求在检测页面内被阻止，图片由本地文件内嵌，未使用公众号登录会话。

- `.state/qa-layout/before.report.json`：旧 HTML 重现 14 处 line-height 警告，首个位置是 paragraphIndex 3，对应截图第 4 段。
- 仅放大行高不能解决混排行内元素的重复行框问题。按照规范添加只包含文字的 `span leaf`，保持粗体、链接、段落和文字顺序，避免段落直接混合裸文字与行内元素；采用明确行高、左对齐和平台默认字体。
- `.state/qa-layout/after.report.json`：实际待粘贴 HTML（图片占位符阶段，去掉外层 section）全规则通过，0 处问题。
- `.state/qa-layout/after-full.report.json`：包含 7 张实际图片的完整图文全规则通过，0 处问题。
- 新增异步粘贴等待、结构警告中止、残缺正文不通过以及混合粗体／链接／引用／列表的上游检测回归测试。

复查：`.venv\Scripts\python.exe scripts/check_wechat_layout.py .state/qa-layout/after.html .state/qa-layout/after-full.html`。报告直接来自上游检测器，未修改引擎、未添加规则豁免。检测失败时命令退出码为 1。

可选检测器位于 `.state/qa-tools/verify-article-structure-spec`；在其 `cli` 目录安装依赖（设置 `PUPPETEER_SKIP_DOWNLOAD=true`，复用本机 Edge），执行 `node build-browser.mjs`。运行产品本身不需要这些 Node 测试依赖。

以上证明导出的本地图文符合当前检出的开源规则；微信服务器实际上传图片、封面裁剪和草稿保存仍不能由此推断为已完成。

0.4.1 修复：最新记录停在标题校验。现场诊断显示可见标题为 ProseMirror DIV，旧 textarea 隐藏。改为读取段落实际文字，忽略光标尾部 BR，保留真正换行和空格；填写后使用 blur 而非 Tab，并等待短暂的页面同步。允许正文为空且已有字段与本篇一致时续填。测试使用带段落、尾部光标 BR 和 Tab 拦截的模拟标题框复现旧逻辑误报；36 项测试通过，真实微信全流程仍待实测。

0.4 修复：从最新失败记录确认标题字段不可见导致正文之后的步骤未执行。增加按可见占位文字识别输入栏、字段完整性预检、富文本 paste 事件、正文段落检查、排除封面／弹窗文件输入的正文上传入口选择、指定封面选择与裁剪流程、保存前封面未变校验。作者已按用户要求写入深度稿配置。控件结构诊断不包含输入值、登录 URL 或 Cookie。

新增本机 Edge 集成测试使用 `tests/fixtures/wechat_editor.html` 模拟编辑器的剪贴板和上传交互，覆盖隐藏旧标题框、可编辑标题、多候选拒绝、字段缺失先停止、重复图片与非首图封面、封面失败／被移除、段落挤成一段及图片丢失／错序。该页面不是微信代码，也没有模拟真实上传接口；测试通过不能证明微信当前富文本 schema、图片上传服务或裁剪对话框已适配。当前工具无法连接用户截图中的专用 Edge，真实账号实测仍待进行。

本次直接在 E:\gitHouse\zhizhou-publisher 修复公众号网页模式：空白检测读取文档内容，忽略已识别的 placeholder／不可编辑提示节点及 ProseMirror 分隔符；普通正文即使与提示文字相同仍受保护。增加阶段错误提示，保留提交中断时的待核实状态，不输出原始异常中的私密网址。新增测试在本机无头 Edge 的本地页面验证空白提示、真实正文、媒体和非破坏性检查，并验证标题保护及中断记录。尚未在真实公众号编辑器验证本次修复后的图片上传、草稿保存和正式发布。

覆盖：

- 文件夹导入 API、图片返回与预览数据；相同内容重复导入复用目录。
- 数字图片排序、Markdown 指定位置、无图片标记的文末图库。
- 本地图片修改导致内容版本变化，远程／越界图片不读取，原始 HTML 不执行。
- 小红书独立短文、超长文不擅自截断。
- 微信稳定令牌、正文图上传、封面上传 type 参数、草稿创建、草稿复用、正式发表与结果查询（模拟平台响应）。
- 微信平台草稿被编辑后停止提交，避免发送不同于本地预览的内容。
- 重复提交保护、跨草稿／发表操作的进行中任务保护、程序中断转为结果待核实。
- 提交请求超时不会自动再提交；平台已受理但查询失败时保持受理状态。
- 本机配置按用户要求明文保存；前端设置响应不包含密钥。
- API 来源／令牌检查，旧预览与缺失凭证不会创建发送任务。
- 小红书控件适配逻辑（模拟页面）：按序上传、输入不一致中止、登录页中止、明确成功提示确认、无成功提示转为待核实。

浏览器验证：已实际打开本地界面，确认示例文章、两张配图、公众号正文、公众号 ID 和未配置密钥状态可见；切换小红书后标题、短文、图片及发送按钮正确切换。

未完成的外部验证：真实微信账号接口权限、真实小红书账号网页上传与发布。没有 AppSecret／扫码会话，未调用真实平台发布接口，未向公众号或小红书发布测试文章。自动化测试不能证明平台当前账号可以正常发布。

项目位置：E:\gitHouse\zhizhou-publisher。本次修改已直接写入该项目，无需复制更新文件。
